@model IEnumerable<DiscoveryCenter.Models.Survey>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<br />
<div class="col-md-offset-2 col-md-8 panel panel-default">
    <div class="panel-body ">
        <h2>Surveys</h2>
        <div class="clearfix">
            <div class="col-md-4" style="padding-left:0px">
                <p>
                    <a class="btn btn-primary" href="@Url.Action("Create","Creation")" id="addItem">
                        <i class='glyphicon glyphicon-plus'></i> Create New Survey
                    </a>
                </p>
            </div>
            <div class="col-md-8 selection-buttons " style="padding-right:0px">
                <div class="btn-group pull-right">
                    <a class="btn btn-default" type="button" href="@Url.Action("Edit", "Creation", new { id = "" })">
                        <span class="glyphicon glyphicon-edit"></span> Edit
                    </a>
                    <a class="btn btn-default" type="button" href="@Url.Action("Duplicate", "Creation", new { id = "" })">
                        <span class="glyphicon glyphicon-duplicate"></span> Duplicate
                    </a>
                    <a class="btn btn-default" type="button" href="@Url.Action("Index", "Home", new { id = "" })">
                        <span class="glyphicon glyphicon-eye-open"></span> View Survey
                    </a>
                    <a class="btn btn-default" type="button" href="@Url.Action("Index", "Report", new { id = "" })">
                        View Report
                    </a>
                    <a class="btn btn-danger" type="button" href="@Url.Action("Delete", "Creation", new { id = ""})">
                        <span class="glyphicon glyphicon-remove"></span> Delete
                    </a>
                </div>
            </div>
        </div>
        
        @*<table class="table">
            <tr>
                <th class="col-md-2">
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th class="col-md-1">
                    Number of Questions
                </th>
                <th class="col-md-2">
                    @Html.DisplayNameFor(model => model.CreateDate)
                </th>
                <th class="col-md-2">
                    @Html.DisplayNameFor(model => model.LastModifiedDate)
                </th>
                <th class="col-md-4"></th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @item.Questions.Count
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreateDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastModifiedDate)
                    </td>
                    <td>
                        <div class="btn-group">
                            <a class="btn btn-default" type="button" href="@Url.Action("Edit", "Creation", new { id = item.Id})" id="@string.Format("edit_{0}",item.Id)">
                                <span class="glyphicon glyphicon-edit"></span> Edit
                            </a>
                            <a class="btn btn-default" type="button" href="@Url.Action("Duplicate", "Creation", new { id = item.Id})" id="@string.Format("duplicate{0}",item.Id)">
                                <span class="glyphicon glyphicon-duplicate"></span> Duplicate
                            </a>
                            <a class="btn btn-default" type="button" href="@Url.Action("Index", "Home", new { id = item.Id})" id="@string.Format("view_{0}",item.Id)">
                                <span class="glyphicon glyphicon-eye-open"></span> View
                            </a>
                            <a class="btn btn-danger" type="button" href="@Url.Action("Delete", "Creation", new { id = item.Id})" id="@string.Format("delete_{0}",item.Id)">
                                <span class="glyphicon glyphicon-remove"></span> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </table>*@

        <ul style="list-style-type: none; margin: 0;padding:0;">
            <li class="header clearfix">
                <div class="col-md-3">
                    <span>@Html.DisplayNameFor(model => model.Name)</span>
                </div>
                <div class="col-md-3">
                    <span>Number of Questions</span>
                </div>
                <div class="col-md-3">
                    <span>@Html.DisplayNameFor(model => model.CreateDate)</span>
                </div>
                <div class="col-md-3">
                    <span>@Html.DisplayNameFor(model => model.LastModifiedDate)</span>
                </div>
            </li>
        </ul>
        <ul class="selectableList">
            
            @foreach (var item in Model)
            {
                <li class="ui-widget-content clearfix">
                    @Html.HiddenFor(modelItem => item.Id, new { @class="id"})
                    <div class="col-md-3">
                        <span>@Html.DisplayFor(modelItem => item.Name)</span>
                    </div>
                    <div class="col-md-3">
                        <span>@item.Questions.Count</span>
                    </div>
                    <div class="col-md-3">
                        <span>@Html.DisplayFor(modelItem => item.CreateDate)</span>
                    </div>
                    <div class="col-md-3">
                        <span>@Html.DisplayFor(modelItem => item.LastModifiedDate)</span>
                    </div>
                </li>
            }
        </ul>
    </div>
</div>

@section scripts
{
    <script>
        $(".selectableList").selectable({
            stop:function(event, ui){
                $(event.target).children('.ui-selected').not(':first').removeClass('ui-selected');
                refreshButtons(event.target);
            }
        });

        $(document).ready(refreshButtons(null));

        function refreshButtons(target)
        {
            if (target != null)
                var selected = $(target).children('.ui-selected').first();
            else
                var selected = null;

            if (selected != null) {
                console.log(selected);
                var id = $(selected).find("input.id").first().val();
                console.log(id);
                $(".selection-buttons a").each(function (index, ele) {
                    $(ele).attr('href', function (i, a) {
                        
                        if (/id=[0-9]*&/.test(a))
                            return a.replace(/id=[0-9]*&/ig, "id=" + id);
                        else {
                            a = a.replace(/\?.*$/, "");
                            return a + "?id=" + id;
                        }
                    });
                    $(ele).removeAttr('disabled');
                    console.log(ele);
                });
                
            }
            else
            {
                $(".selection-buttons a").each(function (index, ele) {
                    console.log(ele);
                    $(ele).attr("disabled", "disabled");
                });
            }

        }
    </script>
}